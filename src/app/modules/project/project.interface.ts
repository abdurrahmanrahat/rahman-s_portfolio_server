export type TProject = {
  projectName: string;
  projectImage: string;
  shortDesc: string;
  features: {
    one: string;
    two: string;
    three: string;
  };
  technology: string;
  liveLink: string;
  githubClient: string;
  githubServer: string;
};
